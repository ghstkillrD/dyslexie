## 🔧 Setup Instructions

1. Clone the repo
path: dyslexie
2. Create virtual env: `python -m venv venv`
3. Activate: `venv\Scripts\Activate.ps1`
4. Install deps: `pip install -r requirements.txt`
(No need to do these since, venv file didn't added to .gitignore)
5. Move to path: dyslexie\fastapi-ml
6. Read the README file and follow the instructions.
